import { IFieldMapConfig } from '../types';
/**
 * Registers a new map with firestorm.
 * @param fieldConfig The field configuration
 */
export default function (fieldConfig?: IFieldMapConfig): Function;
